# Copy this file to .env and fill in real values. .env is gitignored.

# Postgres connection string from Supabase. Use the DIRECT connection (port
# 5432), NOT the pooler (port 6543), for prepare.py — the pooler does not
# support all DDL operations cleanly. Phase 7+ may introduce a separate
# DATABASE_POOLER_URL for the autonomous loop if connection limits become an
# issue. Append ?sslmode=require so TLS is enforced.
DATABASE_URL=postgresql://postgres.gobcpyzxwvxfqfaxxelo:SupabaseParol%21@aws-1-us-west-2.pooler.supabase.com:5432/postgres
# Anthropic API key (only required when AI fallback layer is built in Phase 12).
# Leave blank for Phase 1.
ANTHROPIC_API_KEY=
