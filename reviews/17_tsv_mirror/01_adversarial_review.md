# Tier-2 Adversarial Review (Agent 3) — experiment_log.tsv Postgres durability mirror

> Produced AFTER implementation by an independent fresh-context reviewer
> (general-purpose subagent, session-strongest model: Claude Fable 5)
> that did not author the code, per `STANDING_RISKS.md` § Change tiers.
> Review target: commit `796379d`, verified byte-identical to the
> working-tree diff under review; merged to main via PR #25 (`ef7fa57`)
> mid-review at the operator's instruction (reviews/14 precedent).
> Findings dispositioned in `02_decision_note.md`.

**Session type**: light-tier read-only adversarial review. No files modified. Invariants honored (no edits to `prepare.py`/`parameters.json`/`sources.json`/`program.md`; TSV untouched). **Review target moved under me mid-session**: the working-tree diff was committed as `796379d` ("prepare-mutation: add experiment_log_mirror durability table — metric UNCHANGED"), pushed, and **merged to main via PR #25 (`ef7fa57`) while this review was in flight** — declared in the commit message as the reviews/14 precedent ("adversarial post-review in flight; findings land as follow-up"). I verified the committed content is byte-identical to the diff I reviewed. Repo state: between runs (only `autoresearch/atl-2026-05-05` exists on origin — stale, tip 2026-05-05, fully merged into main; no TSV; no live loop).

## (1) Findings table

| # | Sev | Finding, concrete failure scenario, refutation attempt | Disposition needed |
|---|-----|---|---|
| F1 | **MED** | **The suite-wide mirror kill switch is the SOLE hermeticity barrier for ~513 loop/TSV tests, and it honors an empty-string override.** `tests/__init__.py:17` uses `os.environ.setdefault("EXPERIMENT_LOG_MIRROR_DISABLE", "1")`; an empty value counts as "present", and `runner._mirror_log_row` treats `""` as falsy (enabled) — the exact idiom `tests/test_mirror.py:11-14` documents for re-enabling. Scenario: `EXPERIMENT_LOG_MIRROR_DISABLE= make tests` on a dev machine with a real `.env` → every loop-test TSV append does a real connect and **INSERTs fabricated rows into the live mirror, which has no sanctioned deletion** — permanent pollution of the durability record. Second-order: direct-file execution (`PYTHONPATH=. python tests/test_discovery.py`) bypasses the package `__init__` entirely. Refutation attempt: both require deliberate unusual invocation (plain `python tests/test_X.py` fails at import; `discover`, `-m unittest tests.X`, and pytest all import the package — verified empirically); loop tests do not otherwise patch the mirror (verified: zero `_mirror_log_row`/`MIRROR` references in `test_discovery.py`). Survives as a real one-line-fixable hole: the "operator override" that `setdefault` preserves is precisely the SR-6-violating case. | Follow-up commit: in `tests/__init__.py` treat empty as unset (`if not os.environ.get(k): os.environ[k] = "1"`), or belt-and-braces `mock.patch` of `_mirror_log_row` in loop-test `setUp`. |
| F2 | **MED (process)** | **Merged to main before the Agent-3 review and its artifacts existed.** `reviews/17_tsv_mirror/00_risk_review.md` promises `01_adversarial_review.md` + `02_decision_note.md`; neither exists; G8 explicitly requires the decision note to repeat the metric-comparability carve-out. Also the risk review's repo-state verification ("zero autoresearch/* branches") is false against origin (`autoresearch/atl-2026-05-05` exists — stale and fully merged, so the between-runs conclusion stands, but the stated check was made on an unfetched local view). Refutation attempt: the ordering follows the declared reviews/14 precedent ("fixes landed as an immediate follow-up", `reviews/14_streamlining_review/01_tier2_review_decision.md:9`) and the PR merge constitutes the human-to-main protocol step. Survives as an obligation, not a violation. | Land `01_adversarial_review.md` (this review) + `02_decision_note.md` with the comparability carve-out and F1's fix immediately. |
| F3 | LOW | **Prod durability is currently zero and its first-run failure mode is silent.** `validate-phase1` live-validate fails on this commit (and the prior one) with `FATAL: (ENOTFOUND) tenant/user postgres.gobcpyzxwvxfqfaxxelo not found` — a pre-existing dead DSN secret (fix already in flight on `claude/verify-validate-secret`), so **`experiment_log_mirror` has never been created in prod**. If a future run starts via `make loop`/`make baseline` (not `make daily`) against a DB whose schema predates this commit, every `_mirror_log_row` fails "relation does not exist" — warn-only, forever, invisibly. Refutation: the documented start path `make daily` → `db-check` → `python prepare.py` → `apply_schema` creates the table before the loop, and one later `make mirror-backfill` fully heals any unmirrored stretch (verified count-based reconciliation). Survives only as an ops checkpoint. | After the secret fix merges: confirm live-validate green (that run creates the table). Optional hardening: mirror-table existence as an informational check in `verify_setup`. |
| F4 | LOW | **`runner.py` comment overclaims what `SET LOCAL statement_timeout` fixes.** It says without it "a network partition after connect could hang the loop" — statement_timeout is server-side; a true partition still hangs the client to TCP-stack timescales (minutes). It does bound lock-waits/slow queries to 5s. Refutation: exposure is identical-or-better than every pre-existing DB call in the loop (none set statement_timeout), so no new unbounded-block class; G1's "never blocks unboundedly" holds to the same standard the codebase already accepts. | Cosmetic comment fix next time the file is legally editable; record acceptance in the decision note. |
| F5 | LOW | **Backfill/live-append race can double-insert one row and trip a false tamper alarm.** Backfill snapshots the TSV, then COUNTs; a live append whose mirror insert lands after the COUNT yields one extra mirror copy → next backfill reports `mirror_only > 0`, which SR-16's new text interprets as "out-of-band mirror writes" (tamper). Refutation: window is milliseconds around an append; advisory lock correctly serializes backfills per R-M7's letter; consequence is one benign duplicate flagged by the canary, never a decision input; restore-after-divergence would duplicate one forensic line, and anchors are last-match per-run so decisions are unaffected. Survives only as a docs nuance. | Decision note: name the benign explanations of `mirror_only > 0` (this race; restore-skipped rows) before the tamper interpretation. |
| F6 | LOW | **Static guards are evadable regexes.** Demonstrated by in-memory mutation: the pairing test catches an unpaired bare append and an append/mirror inversion, but `_r.append_experiment_log_row(` (module-prefixed) and aliased calls evade it; `DELETE FROM public.experiment_log_mirror` (schema-qualified) evades the mutation guard. Refutation: zero evasions exist in the tree (grep-verified); these are tripwires against *accidental* regressions, consistent with the repo's other static guards; deliberate evasion is SR-16 territory and backfill heals a forgotten mirror site. | Accept as tripwire; note in decision note. |
| F7 | LOW | **Restore refuses a header-only TSV (size > 0) and the error's remedy is wrong for that state.** A header-only file (only creatable by hand — the writer bootstraps header+row in one write) makes restore raise, directing the operator to `mirror-backfill`, which is a no-op on zero rows; the actual remedy is deleting the file. Refutation: unreachable via any code path; refusing is the conservative correct default for SR-13 (never touch an existing file). Survives as a message nit only. | Accept; optionally mention file deletion in the message on a future legal edit. |

Findings attempted and **refuted** (not in the table): Decimal scale drift breaking dedup (`62.40` vs `62.4` — Postgres NUMERIC compares numerically AND both sides pass through the same canonical formatter; probe confirmed hand-edited `11.2`/`62.40` rows collapse to one key, count once, insert canonical values); `need` going negative or triplet over/under-insert (probe: 3 TSV copies, 1 mirrored → exactly 2 inserts); `fetchone_queue` order nondeterminism (Counter preserves first-occurrence insertion order — guaranteed dict semantics, probe-confirmed); legitimate rows failing `_coerce_tsv_row` (halt rows with `commit=pending`, `metric=0` coerce and backfill — `_TSV_COMMIT_RE` admits `pending`; probe-confirmed); restore round-trip corruption (probe: `Decimal("11.20")→"11.20"`, `Decimal("68.1")→"68.1"`, `Decimal("0.00")→"0.00"`, INT-max metric — exact); marker parsing (last-match + shape validation rejects `run=WEIRD$TAG`, truncated `exp=`, uppercase; the first-match/last-match divergence from `_row_run_tag` is decision-irrelevant since anchors only read baseline/keep rows, which carry no error text); KeyboardInterrupt swallowed by `except BaseException` (deliberate under NEVER STOP; halt is sentinel-based; second Ctrl-C outside the mirror still works).

## (2) Gate verdicts

| Gate | Verdict | Evidence |
|---|---|---|
| G1 | **PASS** | Hostile probes: SystemExit/KeyboardInterrupt/MemoryError/RecursionError from `dsn_available` and `get_connection` all → `False`, nothing escapes; no-DSN → fast `False`; `.env`-as-directory → `dsn_available` returns False without raising; real failed connect → `False` in <1s; kill-switch check precedes everything; blocking bounded to the codebase's existing standard (F4 wording nit). |
| G2 | **PASS** | 635 tests, 0.861s, green with no DATABASE_URL/no `.env`/no network, under `discover`, `-m unittest tests.X`, and CI's exact invocation; kill switch verified set on package import; zero live-mirror writes possible from the suite as invoked (F1 hole requires a deliberate empty-string override or PYTHONPATH direct-file run). |
| G3 | **PASS** | All 4 live sites append-then-mirror; halt path mirrors only after append success (early return on exception — unit-tested); the 5th `append_experiment_log_row` call is inside restore, correctly excluded (writes TSV *from* the mirror); breaker path's halt-row-then-breaker-row ordering matches in TSV and mirror; mutation test confirms the pairing guard catches inversions. |
| G4 | **PASS** | `git diff` hunks in prepare.py are exactly `dsn_available` + DDL + index + `_ALL_DDL` entry; metric functions/CTEs/purge SQL/parameters logic byte-untouched; runner.py decision functions (`apply_keep_or_revert_decision`, `_last_baseline_or_keep`, `_row_run_tag`, `evaluate`) untouched; only DELETE in runner/prepare targets `parcel_scores`; mirror reads = exactly the 2 sanctioned constants (grep + static test). |
| G5 | **PASS** | Table is `experiment_log_mirror`. |
| G6 | **PASS** | Types match `_validate_log_row` output (probe-verified value fidelity); `run_tag`/`experiment_id` nullable; commit_hash TEXT no CHECK (admits `pending` — probe-verified); no status CHECK; `IF NOT EXISTS` + plain `CREATE INDEX IF NOT EXISTS` (not CONCURRENTLY) inside `apply_schema`'s single transaction. Note: DDL has not actually reached prod yet (F3 — dead DSN), so the freeze is nominal, not applied. |
| G7 | **PASS** | `_mirror_log_row` inserts the `_validate_log_row` output; round-trip equality test (`test_mirrored_values_equal_tsv_line`) plus independent probes confirm value-equality including sanitization; backfill markers last-match + shape-validated (tested, probed). |
| G8 | **PARTIAL** | Commit subject/body carry `prepare-mutation:` + "byte-UNCHANGED" enumeration + "TSV metric values remain comparable across this commit" + explicit do-not-apply-not-comparable-rule instruction; main landing achieved via PR #25 merge (declared protocol note). **Unmet**: `02_decision_note.md` repeating the carve-out does not exist yet (F2). |
| G9 | **PASS** | SR-13 (mirror append-only, no sanctioned deletion, purge-exempt), SR-16 (tamper surface + `mirror_only` canary), mechanics durability note + `halt` status vocabulary fix, STORAGE_ARCHITECTURE table + exact restore query all in the same commit; wording preserves "TSV canonical; never a decision input" and matches implemented behavior (F5's canary nuance and mechanics' "MISSING TSV" being slightly narrower than code's exists-but-empty allowance are the only wording deltas — both conservative). |

## (3) Overall verdict: **APPROVE-WITH-FIXES**

The metric and decision logic are verifiably byte-untouched; the mirror is strictly subordinate to the TSV at every site; G1 survived every hostile probe constructed; the dedup, restore round-trip, and marker parsing survived adversarial data-shape attacks. The change is already merged, so the fixes are a follow-up commit:

1. **Required — F1**: make the suite kill switch robust to an empty-string value in `tests/__init__.py` (treat falsy as unset), or patch `_mirror_log_row` in loop-test `setUp`. One line either way.
2. **Required — F2**: land `reviews/17_tsv_mirror/01_adversarial_review.md` (this review) and `02_decision_note.md` restating the metric-comparability carve-out and dispositioning F3–F7, per the risk review's own G8/G9 and the commit's stated promise.
3. **Required — F3 (ops, not code)**: after the DSN secret fix merges, confirm `validate-phase1` live-validate goes green — that run is what actually creates `experiment_log_mirror` in prod; until then the durability feature is inert.
4. Optional: F4 comment wording, F5 canary-nuance sentence, `verify_setup` mirror-table check, F7 error-message remedy.

No REJECT-class defect found: no input, failure injection, or invocation path was constructed by which the mirror raises into the loop, precedes the TSV, feeds a decision, deletes history, or corrupts a restored log.
